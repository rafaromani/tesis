{\color{orange} Can i just copy the definitions from toyota?}

Extra things i need:

Define good symbols for $\maxrep$ y $\minrep$. 

Properties i use later on that i should explain here:
\begin{itemize}
        \item $exp(\minrep) = 0$
        \item $exp(\maxrep) = \maxrep$
        \item $\minrep*\maxrep = \minrep$
        \item $\maxrep*\maxrep = \maxrep$
        \item $x+0 = x$
        \item $x-x = 0$
        \item $x + \maxrep + \maxrep + \minrep =  0$ for all $x$.
\end{itemize}

